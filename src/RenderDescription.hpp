#pragma once

enum E_COLORS
{
    C_RED,
    C_BLUE,
    C_YELLOW
};

class RenderDescription
{
private:
    E_COLORS mColor;
    double * _mX;
    double * _mY;

    double * _mHeight;
    double * _mWidth;
public:

};